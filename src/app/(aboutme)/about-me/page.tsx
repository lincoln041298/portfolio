import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Similarity API | Dashboard",
  description: "Free & open-source text similarity API",
};

const page = () => {
  return <div className="max-w-7xl mx-auto mt-16"></div>;
};

export default page;
